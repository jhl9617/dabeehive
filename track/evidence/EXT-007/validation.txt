# EXT-007 Validation Evidence

Task: EXT-007 — Runs Tree
Date: 2026-05-04 18:20:36 KST
Branch: poc/EXT-007-runs-tree

## Commands

1. `pnpm --filter @dabeehive/server exec tsc -p ../vscode-extension/tsconfig.json --noEmit`
   - Result: passed
   - Notes: completed with no output.

2. `pnpm --filter @dabeehive/vscode-extension compile`
   - Result: passed
   - Notes: extension package TypeScript compile completed.

3. mocked Runs tree provider smoke
   - Result: passed
   - Notes: mocked `vscode` API and `fetch`; verified `dabeehive.views.runs` provider registration, Bearer token header, status grouping order, status tree item, and run child tree item rendering.

4. `pnpm lint`
   - Result: passed
   - Output: `basic lint passed`

5. `rg "OrchestratorRun|listRuns|RunsTreeProvider|Unable to load runs|dabeehive\\.runStatus|dabeehive\\.run|Run \\$\\{formatShortId" apps/vscode-extension/src`
   - Result: passed
   - Notes: found expected run type, client method, provider, context values, and tree error label.

## Scope Notes

- No real VS Code Extension Host was launched; tree behavior was smoke-tested with a mocked VS Code API.
- Run data is read through the REST client and is not persisted locally.
- Approvals tree implementation is deferred to EXT-008.
- Existing unrelated working tree deletion `docs/codex-implementation-rules.md` was not touched.
