# API-016 Validation Evidence

Date: 2026-05-06
Task: API-016 — Common API tests

## Commands

### Script syntax

Command:
`node -c scripts/rest-happy-path-smoke.mjs`

Result:
Passed with no output.

### Root lint

Command:
`pnpm lint`

Result:
Passed.

```txt
basic lint passed
```

### Source/script check

Command:
`rg -n '"test:api"|DABEEHIVE_REST_TOKEN|assertString|assertEqual|authenticated|steps' package.json scripts/rest-happy-path-smoke.mjs`

Result:
Passed. Confirmed:

- `pnpm test:api` command exists
- REST smoke supports optional `DABEEHIVE_REST_TOKEN`
- response assertions exist for IDs/statuses
- output includes `authenticated` and `steps`

### Local server startup

Command:
`DATABASE_URL=postgresql://jongha@localhost:55432/dabeehive_tst_002 pnpm --filter @dabeehive/server exec next start --hostname 127.0.0.1 --port 18086`

Result:
Initial sandboxed run failed with `listen EPERM`; rerun with approval succeeded.

```txt
✓ Ready in 314ms
```

### Health smoke

Command:
`curl -i http://127.0.0.1:18086/api/health`

Result:
Passed.

```txt
HTTP/1.1 200 OK
{"data":{"status":"ok","service":"dabeehive-orchestrator","checkedAt":"2026-05-06T03:02:06.355Z"}}
```

### Common API happy-path command

Command:
`DABEEHIVE_REST_BASE_URL=http://127.0.0.1:18086 pnpm test:api`

Result:
Failed because the local PostgreSQL database is not reachable.

```txt
POST /api/projects failed with HTTP 500
{
  "error": {
    "code": "PROJECT_CREATE_FAILED",
    "message": "Failed to create project."
  }
}
```

Server log:

```txt
Can't reach database server at `localhost:55432`
```

### Local PostgreSQL readiness

Command:
`pg_isready -h localhost -p 55432`

Result:
Failed.

```txt
localhost:55432 - no response
```

Impact:
The common API happy-path test command now exists and starts correctly against the local server, but the DB-backed project -> issue -> run -> approval flow cannot pass until local PostgreSQL is available.
