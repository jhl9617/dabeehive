# SEC-005 Validation Evidence

Date: 2026-05-06 15:42:28 KST
Branch: poc/SEC-005-dangerous-command-denylist

## Commands

### `pnpm --filter @dabeehive/server exec tsc -p ../../packages/shared/tsconfig.json --noEmit`

Result: Passed

Output summary:

```txt
No TypeScript errors.
```

### `pnpm test:sdk`

Result: Passed

Output summary:

```txt
"dangerousCommandDenylist": [
  "destructive_file_operation",
  "git_history_rewrite",
  "auto_merge",
  "deploy_or_infra",
  "secret_access",
  "shell_control_operator"
]
```

The smoke script also checks:

- `vercel deploy --prod` -> blocked
- `kubectl delete deployment app` -> blocked
- `pnpm lint | tee lint.log` -> blocked
- `git reset --hard HEAD` -> blocked
- `gh pr merge 123 --merge` -> blocked
- `cat .env.local` -> blocked
- `pnpm add zod` -> requires_approval
- `prisma migrate deploy` -> requires_approval

### `pnpm lint`

Result: Passed

Output summary:

```txt
basic lint passed
```

### Source Check

Command:

```txt
rg -n "DEFAULT_DANGEROUS_COMMAND_DENYLIST|APPROVAL_REQUIRED_COMMAND_RULES|shell_control_operator|vercel deploy|kubectl delete|pnpm lint \\| tee|requires_approval" packages/shared/src/blocked-command-detector.ts scripts/sdk-adapter-smoke.mjs
```

Result: Passed

### `pnpm track:status -- --task SEC-005 --status verified --dry-run`

Result: Passed

Output summary:

```txt
dry-run: SEC-005 verified -> verified
file: track/MASTER.md
total=131; not_started=5, planned=0, in_progress=0, blocked=2, implemented=6, verified=118, skipped=0
```

## Notes

- No package dependency was added.
- No actual shell executor, approval API, deployment command, or automatic merge behavior was implemented.
- Existing unrelated dirty file `docs/codex-implementation-rules.md` was not touched.
